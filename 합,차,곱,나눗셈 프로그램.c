/*#include<stdio.h>
#include<stdlib.h>
int main(void)
{
	double x;              //실수형 x선언
	double y;              //실수형 y선언
	double sum, difference, product, quotient;

	printf("x의 값을 입력하세요 : ");     
	scanf_s("%lf", &x);              //실수형으로 x입력
	printf("y의 값을 입력하세요 : ");
	scanf_s("%lf", &y);             //실수형으로 y입력

	sum = x + y;                 //sum에 x+y 연산 값을 저장
	difference = x - y;          //difference에 x-y 연산 값을 저장
	product = x * y;             //product에 x*y 연산 값을 저장
	quotient = x / y;            //quotient에 x/y 연산 값을 저장

	printf("x와 y의 합: %.1lf \n", sum);              //sum 출력
	printf("x와 y의 차: %.1lf \n", difference);       //difference 출력 
	printf("x와 y의 곱: %.1lf \n", product);          //product 출력
	printf("x를 y로 나눈 몫 : %.1lf \n", quotient);   //quotient 출력

	system("pause");
	return 0;


}*/